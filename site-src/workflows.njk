---
layout: docs.njk
title: "Workflow tracks: the nine, and what each one skips · baseline"
description: "Reference for the workflow tracks: the nine a human can select at triage, the two sub-tracks selector nodes enter, and the invariants every track is validated against."
heading: "Nine tracks, and<br>what each one skips."
lead: "Reference for the tracks declared in .claude/workflows.jsonl. A track is a dependency graph of workflow phases: triage picks one at the start of a run, and track_guard then enforces that graph's ordering at the write boundary, so a phase cannot be taken out of turn."
updated: 2026-07-30
breadcrumb: ["docs", "reference", "workflow tracks"]
facts:
  - { label: "selectable", value: "9" }
  - { label: "sub-tracks", value: "2" }
  - { label: "declared in", value: ".claude/workflows.jsonl" }
toc:
  - { id: "selectable", label: "Selectable tracks" }
  - { id: "sub-tracks", label: "Sub-tracks" }
  - { id: "invariants", label: "Invariants" }
  - { id: "preconditions", label: "Preconditions" }
  - { id: "selection", label: "Track selection" }
  - { id: "project-tracks", label: "Project-declared tracks" }
---

<h2 id="selectable"><span class="sec-mark" aria-hidden="true">§ I</span> Selectable tracks</h2>

{# The count reads `tracks.shipped`, not `tracks.canonical`: this sentence is a
   claim about what a fresh install receives, so it must come off the pristine
   template rather than whatever this repo's own workflows.jsonl happens to
   carry. tests/track-count-truth.test.mjs pins that distinction. #}
<p class="sec-lead">{{ baseline.tracks.shipped }} selectable tracks ship in the pristine template, and you pick one of them at triage. The node count on each card is the size of that track's graph, which you can read as a rough measure of how much process the track will add to a run.</p>

{# Derived from workflows.jsonl. A track added to that file appears here on the
   next build, and audit-baseline's docsite check fails if it does not. #}
<div class="grid-rule grid-2 lane-grid">
  {% for t in roster.trackDetail %}{% if t.selectable %}
  <div class="cell" id="track-{{ t.id }}">
    <div class="cell-kicker">{{ t.id }}</div>
    <p class="excerpt">{{ t.gloss }}</p>
    <p class="cell-note">{{ t.phases }} nodes{% if t.invariants %} · {{ t.invariants | join(", ") }}{% endif %}</p>
  </div>
  {% endif %}{% endfor %}
</div>

<h2 id="sub-tracks"><span class="sec-mark" aria-hidden="true">§ II</span> Sub-tracks</h2>

<p class="sec-lead">Two tracks carry <code class="tok">selectable: false</code>, which means triage will never offer them to you. They are entered by a selector node sitting inside another track, so you reach them only as a consequence of a choice made further up.</p>

<div class="grid-rule grid-2 lane-grid">
  {% for t in roster.trackDetail %}{% if not t.selectable %}
  <div class="cell" id="track-{{ t.id }}">
    <div class="cell-kicker">{{ t.id }}</div>
    <p class="excerpt">{{ t.gloss }}</p>
    <p class="cell-note">{{ t.phases }} nodes</p>
  </div>
  {% endif %}{% endfor %}
</div>

<h2 id="invariants"><span class="sec-mark" aria-hidden="true">§ III</span> Invariants</h2>

<p class="sec-lead">Each track lists the invariants it meets, and triage checks a track against Article IV before it builds the graph out. If a track breaks one of them, it is rejected with an error naming the rule it failed, so a malformed track can never become a task list.</p>

<div class="grid-rule grid-2 lane-grid">
  <div class="cell">
    <div class="cell-kicker">commits</div>
    <p>The track ends in a commit. It has to carry <code class="tok">/grant-commit</code> before its <code class="tok">commit</code> node.</p>
  </div>
  <div class="cell">
    <div class="cell-kicker">requires_spec</div>
    <p>The track goes through <code class="tok">/spec</code> and the approval gate after it.</p>
  </div>
  <div class="cell">
    <div class="cell-kicker">git_only</div>
    <p>The track needs a git repo. Without one, triage drops it before ranking.</p>
  </div>
  <div class="cell">
    <div class="cell-kicker">requires_swarm</div>
    <p>The track sends out work in parallel, so it needs the swarm approval gate.</p>
  </div>
  <div class="cell">
    <div class="cell-kicker">chore</div>
    <p>Its verify, simplify, integrate and document nodes only run if the diff touches something that needs them.</p>
  </div>
</div>

<h2 id="preconditions"><span class="sec-mark" aria-hidden="true">§ IV</span> Preconditions</h2>

<p class="sec-lead">A track can declare what has to be true before it is even a candidate. The vocabulary is closed: {{ roster.predicates.length }} predicate names, and Article IV invariant I11 rejects a track that uses any other.</p>

{# Names come from src/cli/workflows-validator-predicates.js, the module the
   validator itself reads. The gloss is editorial and sits in
   _data/predicatenotes.json behind a coverage guard, so a predicate added to the
   vocabulary fails the build until it is documented here. #}
<div class="grid-rule grid-2 lane-grid">
  {% for p in roster.predicates %}
  <div class="cell">
    <div class="cell-kicker">{{ p.name }}</div>
    <p>{{ p.note | safe }}</p>
  </div>
  {% endfor %}
</div>

<p>Because preconditions run before ranking, a track that cannot apply never competes at all. An exception is a different thing: it skips a single node inside a track you have already picked, and it is recorded in the workflow state where the audit can find it.</p>

<h2 id="selection"><span class="sec-mark" aria-hidden="true">§ V</span> Track selection</h2>

<p><code class="tok">/triage</code> first classifies how new the request is, as <code class="tok">pattern-copy</code>, <code class="tok">spec-derived</code>, <code class="tok">novel</code> or <code class="tok">ambiguous</code>, and it must cite the evidence behind that call. The default is then the lightest track whose guardrails still cover the risk. Choosing a heavier one requires a named reason, which is recorded in the workflow state.</p>

<p>If the tree is not a git repository, a track marked <code class="tok">git_only</code> drops out before ranking; a track sitting behind a config flag drops out for as long as that flag is off. That is how <code class="tok">power</code> and <code class="tok">org</code> stay out of the picker until you deliberately turn them on.</p>

<p>The chosen track's graph becomes your session task list, and <code class="tok">track_guard</code> enforces its node order on every workflow artifact written from then on.</p>

<p>Two things can skip a node. The first is the <code class="tok">exceptions</code> array that triage records when it creates the workflow; the second is the right-size gate which runs after <code class="tok">tdd</code>. On a small enough diff that gate may drop simplify and document, and it records every skip it makes (it can never drop security, or a core phase).</p>

<h2 id="project-tracks"><span class="sec-mark" aria-hidden="true">§ VI</span> Project-declared tracks</h2>

<p>To declare your own tracks, or to add nodes to the shipped ones, edit <code class="tok">.claude/workflows.jsonl</code> directly. The Article IV invariants bind every track whoever wrote it. Miss <code class="tok">/grant-commit</code> before a <code class="tok">commit</code> node, or leave a cycle in the graph, and triage will reject the track outright.</p>

<p>Because that file is on the never-touch list, install and upgrade both leave it alone. Merging your local edits against tracks added upstream is therefore a manual job, and the upgrade will not attempt it for you.</p>
