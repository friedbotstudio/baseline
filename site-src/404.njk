---
layout: base.njk
permalink: /404.html
pageTitle: "404 · baseline"
description: "This URL maps to no phase in the baseline. Either the page moved, the link is wrong, or you typed a path the router cannot resolve."
subtitle: "/ v0"
active: ""
---
<main id="main" class="marketing not-found">
  <section class="hero">
    <p class="eyebrow">404 · off the workflow</p>
    <h1>Phase not found<span class="accent">.</span></h1>
    <p class="lead">This URL maps to no phase in the baseline. Either the page moved, the link is wrong, or the path you typed never existed. The constitution is fine. The router is not.</p>
    <div class="ctas">
      <a class="btn btn-primary" data-cta="back-to-overview" href="{{ '/' | rel }}">Back to overview <span class="arr">→</span></a>
      <a class="btn btn-secondary" data-cta="install-instead" href="{{ '/install/' | rel }}">Install instead</a>
    </div>
  </section>

  <section class="section">
    <div class="lede">
      <div class="lede-head">
        <p class="section-num">§ I</p>
        <h2>Find your way back</h2>
      </div>
      <p>Pick one. Every link below is a real route in the baseline. The reference pages each carry their own structural diagram so you can verify the claim once you arrive.</p>
    </div>

    <div class="concepts is-2col">
      <a class="concept" href="{{ '/' | rel }}">
        <p class="num">/</p>
        <h3>Overview</h3>
        <p>The landing. Hook count, skill count, phase ordering, consent-gate diagram, the live-typing dev console.</p>
      </a>
      <a class="concept" href="{{ '/install/' | rel }}">
        <p class="num">/install/</p>
        <h3>Install</h3>
        <p>One command, four sentinel paths. Requirements, what lands, what to do after, how to recover from a bad install.</p>
      </a>
      <a class="concept" href="{{ '/cli/' | rel }}">
        <p class="num">/cli/</p>
        <h3>CLI reference</h3>
        <p>Three install modes, the <code>doctor</code> subcommand, the <code>.mcp.json</code> merge semantics, the manifest, the PlantUML jar.</p>
      </a>
      <a class="concept" href="{{ '/hooks/' | rel }}">
        <p class="num">/hooks/</p>
        <h3>Hooks</h3>
        <p>The {{ baseline.hooks.total }} hooks at every tool boundary. Article-by-article enforcement table. Why consent is unforgeable.</p>
      </a>
      <a class="concept" href="{{ '/memory/' | rel }}">
        <p class="num">/memory/</p>
        <h3>Memory</h3>
        <p>Seven canonical files. The lifecycle loop. Re-verification on every cite. Pruning rules.</p>
      </a>
      <a class="concept" href="{{ '/swarm/' | rel }}">
        <p class="num">/swarm/</p>
        <h3>Swarm mode</h3>
        <p>Parallel TDD across isolated worktrees. Decisions live in main context; the single subagent executes pre-decided recipes.</p>
      </a>
      <a class="concept" href="{{ '/skills/' | rel }}">
        <p class="num">/skills/</p>
        <h3>Skills</h3>
        <p>{{ baseline.skills.total }} skills organized into {{ baseline.skills.categoriesWord }} categories. Core skills versus vendored shared globals.</p>
      </a>
      <a class="concept" href="https://github.com/friedbotstudio/baseline">
        <p class="num">gh</p>
        <h3>GitHub</h3>
        <p>Source, issues, releases. File a bug if you arrived here via a broken link in the docs.</p>
      </a>
    </div>
  </section>
</main>
