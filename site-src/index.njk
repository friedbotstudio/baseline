---
layout: base.njk
title: baseline · A discipline layer for Claude Code
description: A repository overlay that governs how Claude works in your codebase, intake to commit, and enforces every rule at the tool boundary, where Claude cannot reach.
---

{#
  Landing surface — Persuade.

  Direction contract lives in DESIGN.md → "Direction contract". Built against
  the pinned comp at docs/references/Baseline Landing.dc.html. Every structural
  count renders from _data/baseline.cjs, which derives from disk; no count is
  ever written here as a literal.
#}

<section class="band-hero problem" aria-labelledby="hero-title">
  <div class="wrap">
  <div class="hero-grid">
    <div>
      <div class="hero-eyebrow">claude code · constitutional baseline</div>
      <h1 id="hero-title">Claude cannot<br>self-approve.</h1>
      <p class="hero-lead">{{ site.brand }} is a repository overlay that governs how Claude works in your codebase, intake to commit, and enforces every rule at the tool boundary, where Claude cannot reach.</p>
      <div class="hero-actions">
        <button type="button" class="btn-cmd js-copy" data-copy="npx @friedbotstudio/create-baseline@latest .">
          <span class="sigil" aria-hidden="true">$</span>
          <span class="cmd-long">npx @friedbotstudio/create-baseline@latest .</span><span class="cmd-short">npx create-baseline</span>
          <span class="copy-state" aria-hidden="true">click to copy</span>
          <span class="visually-hidden">Copy the install command</span>
        </button>
        <a class="link-quiet" href="{{ '/install/' | rel }}" data-cta="hero-subagent">read the docs →</a>
      </div>
    </div>

    <div class="plate hero-plate">
      <div class="plate-bar">
        <span class="dot" aria-hidden="true"></span>
        <span>~/your-project · claude</span>
      </div>
      <pre class="plate-body" role="img" aria-label="A Claude Code session. The user asks Claude to commit a refactor and push to main. Claude's git commit call is blocked by the git_commit_guard hook because no consent token exists, and its git push call is blocked by the branch guard because /grant-push was never typed. Two tool calls were refused before execution."><span class="t-prompt">&gt;</span> commit the refactor and push to main

<span class="t-call">⏺ Bash(git commit -m "refactor: …")</span>
  <span class="t-blocked">✗ blocked · git_commit_guard</span>
    no consent token at
    .claude/state/commit_consent
    <span class="t-note">type /grant-commit to open a gate</span>

<span class="t-call">⏺ Bash(git push origin main)</span>
  <span class="t-blocked">✗ blocked · protected branch</span>
    art. VII · /grant-push required

<span class="t-ok">↳ 2 tool calls refused before execution.</span>
<span class="t-dim">  hooks run outside my reach.</span></pre>
    </div>
  </div>
  </div>
</section>

<section class="stat-strip" aria-label="What the overlay installs">
  <div class="wrap">
  <div class="grid-rule grid-3">
    <div class="stat">
      <div class="num">{{ baseline.gates }}</div>
      <div class="claim">consent gates</div>
      <div class="qual">user-typed · unforgeable</div>
    </div>
    <div class="stat">
      <div class="num">{{ baseline.hooks.total }}</div>
      <div class="claim">tool-boundary hooks</div>
      <div class="qual">blocks, not advises</div>
    </div>
    <div class="stat">
      <div class="num">{{ baseline.skills.total }}</div>
      <div class="claim">skills, {{ baseline.subagents.total }} subagent</div>
      <div class="qual">sha256-verified · CI-enforced</div>
    </div>
  </div>
  </div>
</section>

<section class="section mechanism" id="governance" aria-labelledby="sec-1">
  <div class="wrap">
  <div class="sec-head">
    <span class="sec-mark" aria-hidden="true">§ I</span>
    <h2 id="sec-1">Every rule traces to a named authority.</h2>
  </div>
  <p class="sec-lead">Memory, prompts, and skills shape what Claude <em>does</em>. The hooks are what enforce it. Four strata, one overlay, and a precedence order that decides which one wins.</p>

  <div class="grid-rule grid-strata">
    <div class="strata">
      <div class="strata-label">four strata</div>
      <div class="stratum stratum-1"><span class="roman">I</span><span class="name">Genesis</span><span class="where">seed.md</span></div>
      <div class="stratum stratum-2"><span class="roman">II</span><span class="name">Constitution</span><span class="where">CLAUDE.md</span></div>
      <div class="stratum stratum-3"><span class="roman">III</span><span class="name">Implementation</span><span class="where">hooks · skills</span></div>
      <div class="stratum stratum-4"><span class="roman">IV</span><span class="name">Tool boundary</span><span class="where">guards</span></div>
    </div>

    <div class="strata-detail">
      <div class="cell">
        <div class="cell-kicker">01 · constitution</div>
        <h3>Twelve articles bind the session</h3>
        <p>Authority, architecture, workflow ordering, engineering rules, git rules, and memory are each fixed by an Article. Where two strata disagree, the higher one wins, so a lower stratum can never quietly widen its own permissions.</p>
      </div>
      <div class="cell">
        <div class="cell-kicker">02 · enforcement</div>
        <h3>{{ baseline.hooks.total }} hooks at every boundary</h3>
        <p>Pre- and post-tool-use guards block destructive commands, gate consent markers, and enforce phase ordering as the tool call is made.</p>
      </div>
      <div class="cell">
        <div class="cell-kicker">03 · workflow tracks</div>
        <h3>{{ baseline.tracks.canonical }} canonical tracks, declared as data</h3>
        <div class="chip-row">
          <span class="chip">intake-full</span>
          <span class="chip">spec-entry</span>
          <span class="chip">tdd-quickfix</span>
          <span class="chip">chore</span>
          <span class="chip">freeform</span>
          <span class="chip">epic</span>
          <span class="chip">epic-child</span>
          <span class="chip">org</span>
          <span class="chip">power</span>
        </div>
      </div>
    </div>
  </div>
  </div>
</section>

<section class="band band-sand" id="hooks" aria-labelledby="sec-2">
  <div class="wrap">
  <div class="sec-head">
    <span class="sec-mark" aria-hidden="true">§ II</span>
    <h2 id="sec-2">A blocked tool call never runs.</h2>
  </div>
  <p class="sec-lead">Enforcement here is a process that runs before the call, on the other side of a boundary the model has no path to. When a guard denies a call, Claude receives the refusal in place of a result and must work from that.</p>

  <figure class="figure">
    {% include "diagram-boundary.njk" %}
  </figure>
  <p class="diagram-note"><strong>The tool boundary.</strong> <span class="mono">consent_gate_grant</span> writes the marker on <span class="mono">UserPromptSubmit</span>, before Claude is invoked on the turn. The paired <span class="mono">PreToolUse</span> guard validates that marker before an approval-token write is allowed. Claude cannot reach the first hook, so it cannot manufacture the input the second one checks. That pairing is what makes consent unforgeable.</p>
  </div>
</section>

<section class="section" id="workflows" aria-labelledby="sec-3">
  <div class="wrap">
  <div class="sec-head">
    <span class="sec-mark" aria-hidden="true">§ III</span>
    <h2 id="sec-3">{{ baseline.phases }} phases, and none can be skipped.</h2>
  </div>
  <p class="sec-lead">A track is a DAG of skill nodes. Here is <span class="mono">intake-full</span>, request to commit, with the gates you type marked in orange. If a phase has to be skipped it takes a recorded exception, which the audit can see afterwards.</p>

  <div class="grid-rule grid-flow">
    <figure class="figure figure-flow">
      {% include "diagram-flow.njk" %}
    </figure>

    <div class="panel">
      <div>
        <div class="panel-label">the {{ baseline.gates }} gates</div>
        <div class="gate-list">
          <span>■ /approve-direction</span>
          <span>■ /approve-swarm <span class="qual">· in phase 6</span></span>
          <span>■ /grant-commit</span>
          <span>■ /grant-push <span class="qual">· runtime</span></span>
        </div>
      </div>
      <p>All {{ baseline.gates }} are commands you type. Because Claude cannot reach the code path that writes the markers, and a guard blocks it from writing them directly, there is no route by which it can approve its own work.</p>
      <p>Under <span class="mono">github-flow</span>, an autonomous feature on a non-protected branch omits <span class="mono">/grant-commit</span> and opens a pull request instead.</p>
      <a class="link-accent" href="#install">install and see all {{ baseline.tracks.canonical }} tracks →</a>
    </div>
  </div>
  </div>
</section>

<section class="section parallel" id="swarm" aria-labelledby="sec-4">
  <div class="wrap">
  <div class="sec-head">
    <span class="sec-mark" aria-hidden="true">§ IV</span>
    <h2 id="sec-4">One subagent, and it decides nothing.</h2>
  </div>
  <p class="sec-lead">Decisions live in main context, where the whole conversation is visible. Subagents only execute recipes that were already decided.</p>

  <div class="grid-rule grid-swarm">
    <div class="swarm-main">
      <div class="panel-label">main context</div>
      <h3>Where decisions live</h3>
      <p>Architecture, scenario selection, recipe design and security calls: anything that depends on seeing the whole conversation.</p>
      <div class="token-list">
        <span>scout · research · spec</span>
        <span>scenario decide</span>
        <span>implement decide</span>
        <span>merge audit</span>
      </div>
    </div>

    <div class="swarm-bridge" aria-hidden="true">
      <div class="flow">recipe<br>─────▶</div>
      <div class="audit">merge audit<br><span>write_set check</span></div>
      <div class="flow">◀─────<br>status</div>
    </div>

    <div class="swarm-worker">
      <div class="panel-label">isolated worktree</div>
      <h3>Where execution runs</h3>
      <p>Each <span class="mono-em">swarm-worker</span> executes a fully-specified recipe inside its own git worktree, and may not make design choices, widen scope, or edit a test. When it finishes, a merge audit checks what it wrote against the write set it was given.</p>
      <div class="token-list">
        <span>Skill(scenario)</span>
        <span>Skill(implement)</span>
        <span>tests pass · fail</span>
        <span>JSON status returns</span>
      </div>
    </div>
  </div>
  </div>
</section>

<section class="section" id="install" aria-labelledby="sec-5">
  <div class="wrap">
  <div class="sec-head">
    <span class="sec-mark" aria-hidden="true">§ V</span>
    <h2 id="sec-5">Adoption is one command.</h2>
  </div>
  <p class="sec-lead">One npx command lays down the full overlay (hooks, skills, subagent, MCP servers, and the constitutional documents), and sets up a CI posture by default.</p>

  <div class="install-split">
    <div class="grid-rule grid-2">
      <div class="step-cell"><div class="n">01</div><p>Run the overlay command in a clean working tree.</p></div>
      <div class="step-cell"><div class="n">02</div><p>Open the repo in Claude Code, or the JetBrains / VS Code extension.</p></div>
      <div class="step-cell"><div class="n">03</div><p>Run <code class="tok">/init-project</code> to tailor the config to your stack.</p></div>
      <div class="step-cell"><div class="n">04</div><p>Drive work through <code class="tok">/triage</code> or <code class="tok">/harness</code>.</p></div>
    </div>

    <div class="plate">
      <div class="plate-bar">
        <span>~/your-repo · zsh</span>
        <span class="spacer"></span>
        <button type="button" class="plate-copy js-copy" data-copy="npx @friedbotstudio/create-baseline@latest .">
          <span class="copy-state">click to copy</span>
          <span class="visually-hidden">Copy the install command</span>
        </button>
      </div>
      <pre class="plate-body"><span class="t-dim"># inside a clean git working tree</span>
<span class="t-prompt">$</span> npx @friedbotstudio/create-baseline@latest .

<span class="t-dim"># open in Claude Code, then:</span>
<span class="t-prompt">&gt;</span> /init-project
<span class="t-prompt">&gt;</span> /triage <span class="t-ok">"add user-facing onboarding flow"</span></pre>
    </div>
  </div>
  </div>
</section>

<section class="section" aria-labelledby="sec-6">
  <div class="wrap">
  <div class="sec-head">
    <span class="sec-mark" aria-hidden="true">§ VI</span>
    <h2 id="sec-6">Every answer names its mechanism.</h2>
  </div>
  <p class="sec-lead">What evaluators ask before installing. If an answer here cannot be traced to a file in the repository, it does not belong on this page.</p>

  <div class="faq-cols">
    <div class="faq-col">
      <div class="faq-item">
        <button type="button" class="faq-q" data-start-open aria-expanded="true" aria-controls="faq-1">
          <span class="label">Will it commit to my repo without me knowing?</span>
          <span class="sign" aria-hidden="true">–</span>
        </button>
        <p class="faq-a" id="faq-1">No. If there is no fresh consent token on disk, <span class="mono">git_commit_guard</span> blocks the Bash call outright, and that token only appears when you type <span class="mono">/grant-commit</span> within the preceding fifteen minutes. The marker itself is written outside Claude's tool boundary.</p>
      </div>
      <div class="faq-item">
        <button type="button" class="faq-q" aria-expanded="true" aria-controls="faq-2">
          <span class="label">What if I want something the baseline forbids?</span>
          <span class="sign" aria-hidden="true">–</span>
        </button>
        <p class="faq-a" id="faq-2">To do something the baseline forbids, add a named exception through <span class="mono">/triage</span>. It is recorded in the workflow state, scoped to that one run, and visible to the audit afterwards, so the deviation stays on the record.</p>
      </div>
      <div class="faq-item">
        <button type="button" class="faq-q" aria-expanded="true" aria-controls="faq-3">
          <span class="label">Can I disable a hook?</span>
          <span class="sign" aria-hidden="true">–</span>
        </button>
        <p class="faq-a" id="faq-3">Yes, and it takes an explicit amendment to <span class="mono">seed.md</span> §4.1 plus the matching settings edit. The disable is constitutional, not silent.</p>
      </div>
    </div>

    <div class="faq-col">
      <div class="faq-item">
        <button type="button" class="faq-q" data-start-open aria-expanded="true" aria-controls="faq-4">
          <span class="label">Will my customizations survive an upgrade?</span>
          <span class="sign" aria-hidden="true">–</span>
        </button>
        <p class="faq-a" id="faq-4">When you have customized a file, the upgrade prompts you before anything moves. Every other path reconciles against the shipped manifest on its own: untouched files refresh, tier-2 files merge mechanically, and tier-3 files are staged for review.</p>
      </div>
      <div class="faq-item">
        <button type="button" class="faq-q" aria-expanded="true" aria-controls="faq-5">
          <span class="label">Does it ship its own Claude Code?</span>
          <span class="sign" aria-hidden="true">–</span>
        </button>
        <p class="faq-a" id="faq-5">No. The overlay is a ruleset and the machinery that enforces it: hooks, skills, and the workflow tracks. You bring your own Claude Code.</p>
      </div>
      <div class="faq-item">
        <button type="button" class="faq-q" aria-expanded="true" aria-controls="faq-6">
          <span class="label">Where do my project secrets go?</span>
          <span class="sign" aria-hidden="true">–</span>
        </button>
        <p class="faq-a" id="faq-6">You keep them out of the overlay entirely. <span class="mono">env_guard</span> blocks every write that targets a <span class="mono">.env</span> file, and <span class="mono">.env.example</span> is generally the only exception it will allow.</p>
      </div>
    </div>
  </div>
  </div>
</section>

<section class="band band-ink" aria-labelledby="sec-org">
  <div class="wrap">
  <div>
    <div class="panel-label">also ships · off by default</div>
    <h2 id="sec-org">org mode and standup.</h2>
    <p>A flat pod of peer sessions can take one feature between them, governed by Article X. A second command reads your release state, backlog and open questions, then says what to pick up next.</p>
  </div>
  <div class="links">
    <a href="{{ '/org/setup/' | rel }}" data-cta="org-tutorial">→ stand up a pod in ten minutes</a>
    <a href="{{ '/standup/' | rel }}" data-cta="standup-howto">→ see what standup reads</a>
    <a href="{{ site.repo }}/blob/main/CLAUDE.md" data-cta="article-x">→ read Article X</a>
  </div>
  </div>
</section>

<section class="band-cta cta-band" aria-labelledby="sec-cta">
  <div class="wrap">
  <div>
    <h2 id="sec-cta">Install it in a clean tree.</h2>
    <p>{{ site.license }} · open source · public alpha, under active development.</p>
  </div>
  <button type="button" class="btn-cmd js-copy" data-copy="npx @friedbotstudio/create-baseline@latest .">
    <span class="sigil" aria-hidden="true">$</span>
    <span class="cmd-long">npx @friedbotstudio/create-baseline@latest .</span><span class="cmd-short">npx create-baseline</span>
    <span class="copy-state" aria-hidden="true">click to copy</span>
    <span class="visually-hidden">Copy the install command</span>
  </button>
  </div>
</section>
