---
layout: docs.njk
title: "Velocity: ceremony you can prove you do not need · baseline"
description: "Why the baseline has twelve velocity levers, what the right-size gate measures before it drops a phase, and the rule that keeps a speed-up from turning into a bypass."
heading: "Ceremony you can<br>prove you don't need."
lead: "An eleven-phase workflow is right for a risky feature and absurd for a typo. The velocity levers close that gap. Each one is a config flag. Each removes work a machine can prove is unnecessary, and each stops short of any decision that belongs to you."
updated: 2026-07-29
breadcrumb: ["docs", "concepts", "velocity"]
facts:
  - { label: "levers", value: "12" }
  - { label: "on by default", value: "6" }
  - { label: "config", value: "project.json → velocity" }
toc:
  - { id: "problem", label: "Cost of a fixed pipeline" }
  - { id: "levers", label: "The twelve levers" }
  - { id: "rightsize", label: "Right-size gate" }
  - { id: "boundaries", label: "Lever boundaries" }
  - { id: "failopen", label: "The fail-open default" }
---

<h2 id="problem"><span class="sec-mark" aria-hidden="true">§ I</span> The cost of a fixed pipeline</h2>

<p>A pipeline that treats every change the same has two failure modes, and only one of them is obvious.</p>

<p>The obvious one is waste. Fixing a typo in a comment does not need a simplify pass, a documentation pass and a security review; run them anyway and you will spend ten minutes on a two-line change.</p>

<p>The quieter one does the real damage. When ceremony feels absurd people route around it, and they stop running the workflow for small changes first, then for medium ones. The guardrails end up covering only the work somebody remembered to be careful about, which is the opposite of what guardrails are for.</p>

<p>So the pipeline gives ground deliberately, in the places where a machine can measure that the ground was empty to begin with.</p>

<h2 id="levers"><span class="sec-mark" aria-hidden="true">§ II</span> The twelve levers</h2>

<p class="sec-lead">Every lever is a flag under <code class="tok">project.json → velocity</code>. {{ roster.velocity.onByDefault }} ship on, {{ roster.velocity.offByDefault }} ship off, and you can flip any of them.</p>

{# Lever names and their shipped state are read from src/project.template.json,
   the project.json a fresh install receives, so this table tracks what ships
   rather than what this repo happens to run. The one-line gloss is editorial
   and sits in _data/velocitynotes.json behind a coverage guard. #}
<div class="grid-rule grid-2 lane-grid">
  {% for l in roster.velocity.levers %}
  <div class="cell">
    <div class="cell-kicker">{{ l.name }}</div>
    <p>{{ l.note }}</p>
    <p class="cell-note">ships {% if l.on %}<span class="t-ok">on</span>{% else %}<span class="t-dim">off</span>{% endif %}</p>
  </div>
  {% endfor %}
</div>

<p>The split between on and off is a judgement about blast radius. A lever ships on when its worst case is a slower run, and it ships off when the worst case is a surprise, which is why <code class="tok">power_mode</code>, <code class="tok">org_mode</code> and <code class="tok">sprint_mode</code> all wait for you to ask.</p>

<h2 id="rightsize"><span class="sec-mark" aria-hidden="true">§ III</span> The right-size gate</h2>

<p>The gate runs once, right after your tests go green, and it is the clearest example of the whole idea: it measures the working tree and then decides whether a couple of phases can safely be dropped.</p>

<p>If a diff touches fewer than 4 files and changes 80 lines or fewer, it counts as micro. Both numbers come from <code class="tok">project.json</code> (so you can move them if your changes typically run larger).</p>

<div class="plate">
  <div class="plate-bar">
    <span>what the gate decides</span>
  </div>
  <pre class="plate-body"><span class="t-ok">micro diff</span>            <span class="t-dim">→ skip simplify</span>
<span class="t-ok">micro + no docs touched</span>  <span class="t-dim">→ skip document too</span>
<span class="t-ok">anything else</span>         <span class="t-dim">→ run the full set</span>
<span class="t-ok">security</span>              <span class="t-dim">→ always runs, at every size</span></pre>
</div>

<p>Two kinds of line are taken out of the measure before it decides. Test files come out, because test lines say nothing about how risky a change is, and files that were already dirty when the workflow started come out too, since this workflow did not produce them. What remains is the change the workflow actually made.</p>

<p>The gate is arithmetic all the way down: file counts, line counts, and whether a path matches a glob. No judgement call enters it anywhere, which is what makes it safe to run without asking you first.</p>

<p>It also speaks up when it stays quiet. If <code class="tok">security</code> is being skipped for other reasons and the diff touched a path you marked sensitive, the gate raises an advisory, names those paths, and tells you to run <code class="tok">/security</code> yourself.</p>

<h2 id="boundaries"><span class="sec-mark" aria-hidden="true">§ IV</span> What a lever may never do</h2>

<p class="sec-lead">Three limits keep a speed-up from quietly becoming a bypass. They hold for every lever, present and future.</p>

<p>A lever may only skip from a fixed allowlist. For the right-size gate that list is <code class="tok">simplify</code> and <code class="tok">document</code> and nothing else, so core phases stay put and <code class="tok">security</code> stays put at every diff size. Whether a change needs a security review is a human call, and the gate has no standing to make it.</p>

<p>A lever adds skips and never removes them. If <code class="tok">/triage</code> has already excepted a phase, the gate will leave that alone, because two mechanisms writing the same field in opposite directions is how state goes wrong in a way nobody can reproduce afterwards.</p>

<p>Where a consent gate is involved, no lever reaches it. The three gates are commands you type, checked by a hook which runs before Claude is invoked, and no configuration flag touches that path at all. Turning every lever on therefore leaves the approvals exactly where they were.</p>

<h2 id="failopen"><span class="sec-mark" aria-hidden="true">§ V</span> The fail-open default</h2>

<p>Every lever fails toward the full pipeline. Disable one and you get the whole workflow; break one and you still get the whole workflow, because an error inside the gate returns an empty skip set (fail-open, by design).</p>

<p>That direction is chosen. This machinery can fail in two ways: by running phases you did not need, or by skipping phases you did. The first costs minutes, while the second costs a review that never happened on a change nobody flagged, so every ambiguous case resolves toward doing the work.</p>

<p>The levers read like a performance feature, which undersells them. What they really buy is a workflow that stays usable at small sizes, since a guardrail people route around protects nothing, and the fastest way to get routed around is to be tedious about work that carries no risk.</p>
